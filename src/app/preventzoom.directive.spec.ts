import { PreventzoomDirective } from './preventzoom.directive';

describe('PreventzoomDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventzoomDirective();
    expect(directive).toBeTruthy();
  });
});
